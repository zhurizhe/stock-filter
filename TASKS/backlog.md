# Backlog

- add a screening pipeline that joins `daily` and `daily_basic`
- add CSV caching under `data/`
- add tests around token loading and argument validation
- add a universe screener that feeds `negative_announcement_dates` from公告数据
- add result snapshots and diffing across runs
- add a watchlist mode for `3`-point degraded pullbacks
